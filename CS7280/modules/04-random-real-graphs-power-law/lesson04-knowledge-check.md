# Lesson 4 — Knowledge Check

> Source: Canvas — CS-7280-O01, OAN (Summer 2026)

Ungraded knowledge check. See also [[lesson04-knowledge-check.pdf]].

---

### Q1 — Fill in the Blank (1 point)

If the in-degree distribution of a directed network follows a power law, where $\alpha > 2$, the statements below are FALSE or TRUE?

1. The in-degree cumulative distribution function also follows a power law with the same exponent. — `True` / `False`
2. The in-degree distribution is highly skewed towards values that are much lower than the mean. — `True` / `False`
3. The mean of the in-degree distribution is finite. — `True` / `False`
4. The out-degree degree distribution also follows a power-law distribution. — `True` / `False`

---

### Q2 — Fill in the Blank (1 point)

The following statements below are FALSE or TRUE?

1. To check whether a given network follows a power-law distribution, find the degree range between $k_{sat}$ and $k_{cut}$, and check whether it decreases linearly in a log-linear scale plot. — `True` / `False`
2. The low degree saturation $k_{sat}$ causes a reduction, relative to an ideal power-law form, at low degrees. — `True` / `False`
3. The high degree cutoff $k_{cut}$ is generated by structural constraints and it causes a deviation from an ideal power-law at very high degree values. — `True` / `False`
4. To check whether a given network follows a power-law distribution, rescale the distribution by $k - k_{sat}$, consider the range up to $k_{cut}$, and examine if the rescaled distribution decreases linearly in a log-log plot. — `True` / `False`

---

### Q3 — Multiple choice (1 point)

Let $G$ be a power-law network with 9 nodes. If its minimum degree $k_{min}$ is 4 and $\alpha$ is 3, find the maximum degree $k_{max}$ of $G$.

- [ ] 12
- [ ] 13
- [ ] 15
- [ ] 16

---

### Q4 — Multiple choice (1 point)

The leftmost graph below is the original network. Which one of the graphs at the right side can be categorized as degree-preserving randomization?

![[img/kc-q04-degree-preserving-randomization.png|600]]

- [ ] A
- [ ] B
- [ ] C
- [ ] D

---

### Q5 — Fill in the Blank (1 point)

The following statements about the Configuration Model are FALSE or TRUE?

1. It can produce power-law networks for any value of $\alpha > 2$. — `True` / `False`
2. It may produce a graph with self-loops and/or multi-edges. — `True` / `False`
3. The only requirement is that the node degrees are non-negative integers. — `True` / `False`
4. It can generate a network with any degree sequence. — `True` / `False`

---

### Q6 — Multiple choice (1 point)

In the link selection model, what is the relationship between the degree of an existing node $v$ and the probability that a new node connects to $v$?

- [ ] Exponential
- [ ] Linear
- [ ] Quadratic
- [ ] No relationship between the two
